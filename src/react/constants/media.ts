/**
 * Base URL for static media (article PDFs, etc.) served from the Cloudflare R2
 * bucket `mondesa` via its custom domain. Article PDFs live under `/articles/`.
 */
export const MEDIA_BASE_URL = 'https://media.mondesa.org';
